1. /api/message/getUnionId 可移动到onLoad中
2. /api/employee/getWeekDurationRank 可以去掉，门店排名改为固定显示 让ai查询下
	- 只从首页跳转过去的多不多，不多的话删除掉这个功能 再确认
3. /api/course/getFreeCourse 可以去掉 金刚位固定显示
4. /api/account/userInfo 接口中包含了权限数据
	- 直接删除 hasAwaitExamAndAssess接口调用
5. hasManager 接口中先判断是否有权限，无权限后调用接口查询
6. /api/planRecord/mineV2 /api/planRecord/count 逻辑合并，一并显示
7.  /api/employee/expiredUsers 接口移除，只通过前台通过 可以去掉 使用EXPIRE_NOTIFICATION 判断下常显
8. bigpizza进入小程序时，发送大量的access请求 图片确认是否可以使用懒加载

确认结果
- courseDetail页面的阅读完课件、考试怎么刷新的进度
	- onShow发送请求刷新的数据
- coursedetail里阅读了xxx文件，然后判断下前后的diff，有变化再去请求对应接口
- expireFriend/expireFriend 小程序路由判断下，哪些租户使用了 07-01 ～ 07-08 10点
	- amsz 使用 1 次，共计 1 人
	- hmyz 使用 2 次，共计 1 人
	- mingji 使用 3 次，共计 3 人
	- sanchushan 使用 49 次，共计 29 人
- EXPIRE_NOTIFICATION 权限哪些租户开启了
	- funai、dazhi、demo、dwxs、guoqiaoyuan、amsz、test、panglaohan、xinshuguo、fairwood、chaohaowei、xlxc、tomacado、zzxy、fairwoodhk、zhenghuangqi、qnj、xiaoboye、amsztest、luneurs、shiwei、oldz、hmyz、mojie、fdbs、tangwushaokao、sanchushan、aeon、bfyx、teg、yuehui、lifuji、HLF、mingji、baheli、chilis、cuizhiji、hezhixian、SKYLINE-CATERING、yiyemishan、bigpizza、hwhyb、wshy、lanhai、lyld、fanku、daxiji、cubic3、ymt、rsj、gz-saizeriya、PEAK、ocean、nonggengji、yunhaiyao、syx、dbdpsct、hefanlao、lxcy、YDX1、weishang、suohaha、wh-saizeriya、yaruki、cxq



